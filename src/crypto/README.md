# Crypto

Cryptographic operations in VGBL generally involve decrypting and validating segments before they can be used. Software implementations (flash key storage, CPU validation) are provided, but HAL-specific implementations can be added to use cryptographic hardware onboard the chip.
