# Validation

Cryptographic/checksum operations in VGBL generally allow validation of segments before use. Software implementations (flash key storage, CPU validation) are provided, but HAL-specific implementations can be added to use cryptographic hardware onboard the chip.
